#!/usr/bin/env python3
"""
Setup script for cs-proxy.

Modern Python packaging uses pyproject.toml, but this setup.py is provided
for compatibility with older pip versions and editable installs.
"""

from setuptools import setup

# Configuration is in pyproject.toml
# This file is primarily for backwards compatibility
setup()
